#include"kuis2.h"
int main(){
    int i,n;
    int space=0;

    //Scan Array
    scanf("%d",&n);
    
    //Input
    for(i=0;i<n;i++){
        scanf("%s",a[i]);
    }
    //Space Election
    spaceElect(n,space,a){

    }
    // Printing
    arrayPrint(n,a);

    system("pause");
    return 0;
}